steps:
  - label: inspect-world
    directions: |-
      Inspect every current lensed representation and all related stored
      information supplied to this Reflection. Treat current representations
      and Lens-authored stored episodes as observations. Treat stored artefacts
      declaring the generalisations Reflection as prior generalisations.
      Revisit current and related observations together with prior
      generalisations. Describe how the observations reinforce, qualify,
      conflict with, or connect the prior generalisations, and where they call
      for a new generalisation.
    output:
      inspection: "string"

  - label: evolve-generalisations
    directions: |-
      Evolve the generalisations in light of this inspection:

      {{inspection}}

      Carry forward, combine, broaden, narrow, split, replace, or otherwise
      revise generalisations as observations warrant. Each evolution is one
      current generalisation. Include the exact content and level of every
      prior generalisation that influences it in `evolves_from`. Use an empty
      `evolves_from` for a new generalisation that no prior generalisation
      influences. Exclude prior generalisations that do not influence the
      evolution.
    output:
      evolutions: "Array<{ content: string; evolves_from: Array<{ content: string; level: integer }>; reasoning: string }>"

  - label: synthesise-artefact
    directions: |-
      Synthesize the final generalisation artefact from these evolutions:

      {{evolutions}}

      Include every evolved generalisation. A generalisation with an empty
      `evolves_from` has evolution-depth level 1. Otherwise its level is one
      greater than the highest level in `evolves_from`. Preserve each
      evolution's exact `evolves_from` content and level snapshots in the final
      result.
    output:
      generalisations: "Array<{ content: string; level: integer; evolves_from: Array<{ content: string; level: integer }> }>"
